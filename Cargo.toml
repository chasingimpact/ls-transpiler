[package]
name = "ls-wrapper"
version = "0.1.0"
edition = "2021"
description = "Lightweight ls transpiler for Windows"
license = "MIT"

[dependencies]
# Zero external dependencies

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
